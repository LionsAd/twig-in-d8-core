{#
/**
 * @file
 * Default theme implementation to display a item in a views RSS feed.
 *
 * Available variables:
 * - title: RSS item title.
 * - link: RSS item link.
 * - description: RSS body text.
 * - item_elements: RSS item elements (pubDate, creator, guid).
 *
 * @see template_preprocess()
 * @see template_preprocess_views_view_row_rss()
 *
 * @ingroup themeable
 */
#}
  <item>
    <title>{{ title }}</title>
    <link>{{ link }}</link>
    <description>{{ description }}</description>
    {{ item_elements }}
  </item>
